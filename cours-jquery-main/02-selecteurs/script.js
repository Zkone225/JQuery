// LES SELECTEURS EN jQuery

$(document).ready(function(){

    //cibler un element html
    // $("p").hide();

    //cibler un element html a partir de son id
    // $("#test").hide();

    //cibler un element html a partir de sa classe
    // $(".maClasse").hide();

//cibler un element html specifique
    $("p:first").hide();
    $("li:first").hide();
});
